@AGENTS.md

# mywebsite — 개인 대시보드

## 프로젝트 개요
개인용 대시보드 웹사이트. 상단 탭 네비게이션으로 여러 기능을 오가며 쓰는 단일 페이지 형태.

## 기술 스택
- Next.js
- TypeScript
- Tailwind CSS
- 배포: Vercel (모든 탭 완성 후 진행 — 지금은 배포 설정 작업하지 않음)

## 탭 구조 (상단 네비게이션)
1. **home** — 오늘/날짜별 할 일·일정
2. **calendar** — 달력 뷰
3. **study** — 공부 관련 데이터 (Notion 연동)
4. **files** — 파일 관리
5. **memo** — 메모

## 디자인 방향
- 글래스모피즘(glass morphism): 반투명 + 블러(backdrop-blur) 패널
- Apple.com 스타일 참고: 부드러운 그림자, 넉넉한 여백, 산세리프 폰트, 은은한 그라데이션 배경
- 전체적으로 미니멀하고 여백을 충분히 두는 톤

## 데이터 관리
### home 탭
- 브라우저 **localStorage**에 저장
- 날짜별로 일정/할 일을 보관하는 구조 (날짜를 key로 사용하는 형태 권장)

### study 탭
- **Notion API** 연동으로 데이터를 읽어옴 (읽기 전용으로 시작)
- Notion API 토큰 등 민감 정보는 반드시 `.env.local`에서 관리
- **코드에 API 토큰/시크릿을 절대 하드코딩하지 말 것**
- `.env.local`은 `.gitignore`에 포함되어야 함
- 실제 Notion 연동 작업 전, 어떤 데이터베이스/페이지 구조를 읽어올지 확인 필요

### calendar / files / memo 탭
- 아직 데이터 저장 방식 미정 (구현 시점에 논의)

## 작업 원칙
- 배포(Vercel) 관련 설정은 모든 탭 기능이 완성된 뒤에 진행
- 시크릿/토큰은 항상 환경 변수로만 다루고, 커밋되지 않도록 주의
